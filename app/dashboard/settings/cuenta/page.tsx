import SettingsShell from "@/components/settings/SettingsShell";

/** Ruta canónica de la pestaña Cuenta y tema. */
export default function CuentaSettingsPage() {
  return <SettingsShell initialTab="cuenta" />;
}
