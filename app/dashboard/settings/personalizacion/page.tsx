import SettingsShell from "@/components/settings/SettingsShell";

/** Ruta canónica de la pestaña Personalización (secciones del menú lateral). */
export default function PersonalizacionSettingsPage() {
  return <SettingsShell initialTab="personalizacion" />;
}
